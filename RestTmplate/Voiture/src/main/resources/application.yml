spring:
  application:
    name: SERVICE-VOITURE
  datasource:
    url: jdbc:mysql://localhost:3306/VoitureService
    username: root
    password: "root"
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8089

eureka:
  instance:
    hostname: localhost
